# Auto detect text files and perform LF normalization
* text=auto

# Binary assets must NOT be touched (prevents CRLF corruption of images)
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.webp binary
*.ico binary
